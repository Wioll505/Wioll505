ssh -p (port number) user@IP(or DNS)

if you want to open a file starting with "-" you need to specify the full directory location 
ie: cat /home/-

if you want to open a file with spaces like "file name.txt" you must use single or double quotations
ie: cat 'file name.txt', or if it has a -: cat /home/'-file name.txt'
